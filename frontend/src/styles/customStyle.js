export const customStyles = {
  control: (base) => ({
    ...base,
    border: "none",
    boxShadow: "none",
    borderRadius: "8px",
    backgroundColor: "#F9FAFB"
  }),
  valueContainer: (base) => ({
    ...base,
    padding: 0
  }),
  multiValue: (base) => ({
    ...base,
    backgroundColor: "#e5e7eb"
  })
};
